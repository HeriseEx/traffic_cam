# Android 交通违法智能取证与举报系统实施方案

**个人使用场景 · 手机初筛 + 服务器终判 + 微信侧提交**

版本：V1.4
日期：2026-09

---

## 2026-09-18 进度：闯红灯候选闭环已接通，微信提交仍关闭

当前代码及验收方法见 `IllegalCapture/README.md`、`backend/README.md`。完整系统目标仍见后续章节，不代表已经完成。

**相对第 4 节阶段规划，现在停在 Phase 2 的红灯候选 + 环形缓存 + 服务器独立复核。** 停止线标定和微信提交都还没做。

| 能力 | 状态 | 说明 |
|---|---|---|
| 手机车辆粗筛 | 已完成 | EfficientDet-Lite0，真机可用；夜景默认 40% 阈值会漏检 |
| 环形缓存与标记上传 | 已完成 | 触发前约 10–15 秒短片段 + 触发后 10/15/30 秒，拼好再入队 |
| 自动截取闯红灯候选 | 已完成 | 稳定红灯 ∧ 前车底边仍下移 ∧ 车牌多帧一致；60 秒冷却 |
| 服务器异步分析与回传 | 已完成 | Docker + SQLite 队列；人工复核写入后手机会同步 |
| 车牌检测与 OCR | 已完成 | HyperLPR3；1080p 分块 + 小车牌放大；多帧投票 |
| 手机画面叠加车型+车牌 | 已完成 | 实时约 2.5 秒一帧；近 4 次按位置投票纠正省份误读 |
| 红绿灯状态机 | 已完成感知 | 夜间 HSV 灯芯，连续 3 帧同色才切换；实时帧回传灯色 |
| 闯红灯候选 | 候选不是证明 | 三条件同时成立才给 `RED_LIGHT`；`submission_allowed=false` |
| 红灯期间前车接近 | 观察 | 记录前车底边是否下移；移动机位会一起前移，故不是越线 |
| 固定机位压线/逆行候选 | 骨架 | 必须手工标定且相机静止 |
| 停止线 / 车道线 | 未开始 | 越线的几何参照 |
| 微信提交 | 未开始 | `submission_allowed=false` |

夜间行车记录仪 `test.mp4` 00:00 路口段：灯色在 1.5 秒进入稳定红灯；用于验证服务器候选与上传/复核回传。1:07 跟车段无近处交通灯。录像和识别出的车牌只留在本地 `test/`，不进入仓库。

- **自动优先。** 片段上传后自动排队、分析并回传，人工复核不是处理前提。原始 AI 结果与人工覆盖分开保存。
- **车牌优先，已从“能检出”进入“压单帧误读”。** 不能把缩小到 720p/640 的画面当作唯一 OCR 输入。
- **违法候选保持边界。** 车辆存在不等于违法；红灯稳定不等于闯红灯；移动机位不能用画面轨迹判断压线或逆行。
- **阶段差异明确。** 无停止线自动检测、无后台持续录像（离开画面即停环形缓存）、无微信提交。

### 下一步：停止线标定，以及微信提交门控

环形缓存已经能带上变灯前几秒。三条件（红灯稳定 ∧ 继续接近 ∧ 车牌多帧一致）只产生候选。要证明越线仍需停止线；`submission_allowed` 在接入微信前保持 false。

---

# 1. 文档目标与总体原则

本方案用于指导一套个人使用的 Android 端交通违法智能取证与举报系统落地。系统持续录像，但不持续把完整视频交给服务器深度分析，而是在手机端运行轻量模型进行粗筛；只有疑似违法事件才形成任务包并上传服务器，由服务器执行独立的二次验证和最终判定。

系统采用“**手机负责发现，服务器负责确认，提交层负责执行，记录层负责追踪**”的总体结构。手机与服务器对违法类型判断一致、证据质量满足要求后，任务才允许进入微信侧提交流程；判定不一致或服务器拒绝的任务不提交。

整个项目采取分阶段验证方式推进。第一阶段不追求复杂违法行为识别精度，而优先证明端侧粗筛、任务生成、服务器通信、缓存队列、服务器模型验证、微信提交和结果回传能够形成完整闭环。只有链路稳定后，才继续投入模型特化和复杂违法行为识别。

服务器端从第一阶段开始采用 Docker 化部署，模型、配置、任务数据和程序镜像相互解耦，保证能够在 Oracle 服务器、其他云服务器或本地计算机之间快速迁移。

---

# 2. 总体系统架构

| 层级 | 主要职责 | 关键输出 |
|---|---|---|
| Android 采集层 | 连续录像、环形缓存、轻量检测、人工触发、事件切片 | 候选事件任务包 |
| Android 初筛层 | 车辆检测、轻量跟踪、候选违法判断、证据质量预检 | 手机初筛结果 |
| 服务器任务层 | 接收任务、持久化、排队、状态管理、失败恢复 | 可追踪任务状态 |
| 服务器判定层 | 车辆/信号灯/车道/车牌分析、多帧 OCR、时序规则、模型复核 | 最终通过/拒绝结果 |
| 微信提交层 | 打开举报入口、填写信息、选择视频、提交并读取反馈 | 提交结果 |
| 记录与清理层 | 保存举报记录、证据、判定结果和必要日志 | 可查询、可清理的历史记录 |

核心业务链路为：

**连续录像 → 手机轻量粗筛 → 事件任务包 → 服务器异步队列 → 服务器终判 → 手机接收结果 → 双端一致性检查 → 微信提交 → 获取反馈 → 服务器保存举报记录。**

---

# 3. 分阶段验证策略

项目不采用“一次把所有模块全部做完”的方式，而采用逐级验证。每一个阶段都必须能独立验收，避免录像、AI、网络、服务器和微信任何一个环节出问题后无法定位责任模块。

## 3.1 第一阶段：端到端可行性验证

第一阶段的目标不是获得高识别率，而是证明完整技术链路能够长期、稳定、可恢复地运行。

### 1.1 Android 或网页运行精简模型进行粗筛

优先验证轻量车辆检测能力。网页版本可用于快速验证模型效果和阈值，Android 版本用于验证真实手机上的性能、功耗、录像与推理并行能力。

第一阶段只要求识别车辆相关基础类别，例如 car、truck、bus、motorcycle，并结合简单目标跟踪产生候选任务。此阶段不要求手机准确判断复杂违法行为，只需要证明手机能够在持续录像过程中发现目标、保存事件前后画面并生成标准任务包。

最终正式系统以 Android 端为主，网页端仅作为模型验证、调试和可视化工具。

### 1.2 测试任务提交与反馈流程

提交流程必须与 AI 模型解耦。第一阶段可以使用固定测试视频或人工构造任务，不依赖真实违法识别结果，单独验证以下链路：

**任务进入提交队列 → 打开微信 → 进入举报页面 → 填写必要信息 → 选择证据视频 → 提交 → 获取成功、失败或需要人工确认的反馈 → 将结果回传服务器。**

提交状态至少区分待提交、提交中、成功、失败、等待用户处理和可重试等状态。系统不得把“点击提交按钮”视为举报成功，必须识别最终结果页面或举报记录反馈。

### 1.3 服务器通信、数据缓存与队列管理

服务器第一阶段即采用异步任务模型。手机上传任务后，服务器立即返回任务编号，不要求手机保持一个长时间等待模型完成的 HTTP 请求。

服务器需要完成：任务接收、任务数据落盘、数据库状态记录、排队、Worker 调度、失败重试、结果持久化、客户端结果查询和过期清理。

建议第一阶段使用轻量技术栈，不引入不必要的分布式组件。个人单设备场景下，FastAPI 或等价 API 服务、SQLite 或 PostgreSQL、本地文件系统和一个或少量 Worker 已足够。只有实际任务规模明显增长时，再考虑 Redis、消息队列或更复杂的调度系统。

### 1.4 服务器数据验证模型

服务器模型第一阶段只验证完整处理能力，不追求覆盖全部违法行为。最低目标是能够完成视频解码、车辆检测、目标跟踪、车牌候选识别、基础场景分析和标准化结果输出。

服务器返回给手机的是稳定的业务结果，而不是暴露内部模型实现。手机只关心任务是否通过、违法类型、车牌结果、最终置信度、拒绝原因和证据质量状态。服务器以后可以替换 YOLO、OCR、规则模型或视觉模型，而不改变手机协议。

### 1.5 Docker 部署与迁移验证

服务器端从第一阶段开始全部容器化。API、Worker、模型运行环境和依赖通过 Docker 镜像固定，任务数据、模型文件、数据库和日志通过外部 Volume 或宿主机目录挂载。

第一阶段必须验证：服务器重新启动、容器重新创建、镜像升级后，未完成任务和历史记录不会丢失；同一套部署文件能够迁移到另一台 Linux 服务器运行。

---

# 4. 后续阶段规划

| 阶段 | 目标 | 主要验收内容 |
|---|---|---|
| Phase 1 | 端到端链路验证 | 粗筛、任务包、通信、队列、模型、微信提交、Docker 全部跑通 |
| Phase 2 | 基础视觉能力 | 车辆、跟踪、车牌、红绿灯、停止线、车道线稳定工作 |
| Phase 3 | 基础违法规则 | 闯红灯、明显跨实线、逆行、特定车道占用等规则上线 |
| Phase 4 | 模型领域特化 | 使用真实道路难例对车辆、红绿灯、车牌、车道模型做微调 |
| Phase 5 | 复杂时序行为 | 未打转向灯变道、危险变道、加塞等行为模型 |
| Phase 6 | 长期运行与自动化稳定性 | 微信适配、失败恢复、重试、历史记录、自动清理、健康检查 |

第一版完整业务 MVP 建议只选择一种违法行为打通，优先选择闯红灯。这样可以先验证“红灯状态 + 停止线 + 车辆轨迹 + 证据完整性 + 车牌质量”的完整方法，而不是同时维护多种尚未验证的规则。

---

# 5. 循环录像与跨分段事件处理

循环录像必然会遇到违法过程跨越两个甚至多个物理文件。这个问题必须从录像存储结构上解决，而不是事件发生后再临时拼接文件。

## 5.1 推荐方式：统一时间轴 + 短分片环形缓存

手机持续通过硬件编码器生成 H.264 或 H.265 数据，并以短分片形式写入环形缓存。每个分片记录精确开始时间、结束时间、关键帧索引和编码参数。

事件在 T 时刻被模型或人工触发后，系统按照统一时间轴保留事件前若干秒和事件后若干秒的数据，例如保留 T 前 8–15 秒和 T 后 12–25 秒。

物理分片只是存储实现细节，业务层始终把事件看作一段连续时间轴。

## 5.2 跨分片事件处理

| 情况 | 处理方式 | 说明 |
|---|---|---|
| 事件位于单一分片 | 直接引用或导出目标时间段 | 最简单情况 |
| 事件跨两个分片 | 按时间顺序组合编码数据 | 不要求重新编码 |
| 事件跨三个以上分片 | 根据统一时间轴收集全部相关分片 | 分片数量不影响事件逻辑 |
| 事件文件超过大小限制 | 优先缩短无关前后缓冲 | 避免破坏车牌画质 |
| 平台要求保留未处理原视频 | 保存原始分片及哈希 | 提交策略以平台实际规则为准 |

事件导出优先采用不改变画面像素内容的重封装方式。检测框、OCR 文字、锐化、超分辨率、裁剪和其他 AI 可视化结果只用于内部分析，不应写入证据本体。

---

# 6. Android 端设计

## 6.1 录像与缓存

主证据流建议使用 1080p、30fps、H.264 硬件编码。初始码率可设置为约 4.5–6 Mbps，并设计为可配置。

环形缓存建议保留最近 45–90 秒。正常情况下旧分片自动覆盖；事件触发后冻结对应时间范围，直到事件任务完成、过期或被清理。

事件视频通常控制在约 20–35 秒，具体长度按违法类型决定。对于闯红灯，需要保留红灯状态、车辆接近停止线、越线、继续驶入路口以及违法后状态。

## 6.2 手机轻量粗筛

手机端不承担最终判决，而负责过滤绝大多数普通录像。建议使用 Android 友好的轻量模型和推理框架，例如精简 YOLO 系列配合 TFLite、NCNN、MNN 或其他移动端推理框架。

第一阶段只需要车辆检测与简单跟踪。后续逐步加入红绿灯、车道线、停止线、车牌质量等能力。

手机输出的是“候选事件”，不是“最终违法成立”。

## 6.3 人工提前触发

人工触发用于用户提前观察到可疑行为时冻结当前事件时间窗。由于环形缓存已经存在，即使用户在违法动作开始后才触发，也仍能保留触发前若干秒的视频。

人工触发必须以驾驶安全为前提。驾驶员本人不应在驾驶过程中手持操作手机，可考虑乘客操作、蓝牙按键、车载控制或其他安全输入方式。

## 6.4 录像与微信自动化并行

录像核心应运行于 Android 前台服务中，避免微信被打开后录像 Activity 进入后台就停止采集。

Recorder、事件识别和微信 SubmissionController 逻辑上相互独立。微信正在提交事件 A 时，录像系统仍可继续检测事件 B，并将 B 放入提交队列。微信侧始终按单任务顺序处理，不并发操作多个举报页面。

---

# 7. 事件任务包

手机通过初筛后才生成任务包。任务包必须把原始证据和 AI 元数据分开。

| 内容 | 用途 |
|---|---|
| event_id | 全链路唯一事件标识 |
| 原始事件视频或原始分片集合 | 待验证证据 |
| 手机候选违法类型 | 提供服务器分析入口，不作为最终结论 |
| 手机置信度 | 用于统计和筛选 |
| 事件开始/结束时间 | 服务器时序分析 |
| 关键时间点 | 例如候选越线时刻 |
| 质量指标 | 模糊度、曝光、车牌尺寸、可见帧数量等 |
| 文件哈希 | 校验上传前后证据一致性 |
| App/模型版本 | 便于后续定位不同版本效果 |

任务包上传成功后，服务器生成独立 task_id，并建立 event_id 与 task_id 的关联。

---

# 8. 服务器通信与异步任务设计

服务器必须采用异步处理方式。手机上传成功后立即取得任务编号，服务器在后台排队和推理。

建议任务状态至少包括：

| 状态 | 含义 |
|---|---|
| RECEIVED | 任务已收到并完成基础校验 |
| QUEUED | 已进入待处理队列 |
| PROCESSING | Worker 正在分析 |
| APPROVED | 服务器认为可进入后续一致性检查 |
| REJECTED | 服务器拒绝 |
| ERROR | 处理异常，可根据错误类型决定是否重试 |
| EXPIRED | 任务超过处理或保留期限 |
| SUBMITTING | 手机正在进行微信提交 |
| SUBMITTED | 已确认提交成功 |
| SUBMIT_FAILED | 微信提交失败 |

手机客户端可以使用定时查询、长连接或其他适合移动网络的方式获取结果，但不能依赖一次长连接从上传一直阻塞到 AI 分析结束。

服务器重启后必须能够从数据库恢复未完成任务。任务文件只有在数据库记录成功后才允许进入队列，避免出现“队列有任务但文件不存在”或“文件存在但数据库不知道”的幽灵状态。

---

# 9. 服务器模型与最终判定

服务器承担比手机更完整的视觉分析，并保持与手机初筛相对独立。

推荐处理流程包括：视频完整性检查、关键帧抽取、车辆检测、车辆跟踪、车牌检测、多帧 OCR、红绿灯/停止线/车道分析、时序规则判断、复杂场景复核和最终证据质量检查。

服务器只返回标准业务结果，不要求手机了解内部用了什么模型。

## 9.1 多帧车牌识别

车牌不能只依赖单帧 OCR。服务器应跟踪目标车辆，从多个时间点选取清晰车牌区域，再通过多帧一致性获得最终结果。

如果多个高质量帧无法形成稳定一致的车牌字符串，服务器应降低证据质量或直接拒绝，而不是强行猜测一个最像的号码。

## 9.2 闯红灯优先采用状态机和几何规则

第一版不建议训练一个直接输出“是否闯红灯”的黑箱分类模型。更适合的方式是组合红灯状态、车辆轨迹、停止线位置和越线后的持续运动，形成可解释的时序状态机。

服务器应记录关键依据，例如红灯稳定时间、车辆越线时刻、目标轨迹连续性、越线后持续前进情况和车牌多帧一致性。这样后续调试能够明确知道系统为什么批准或拒绝任务。

---

# 10. 公共模型与模型特化策略

模型训练采用“**先公共模型，后真实难例特化**”的策略，不在项目第一阶段就训练一个统一的违法识别模型。

## 10.1 第一阶段

直接使用公开预训练模型完成车辆检测、跟踪、车牌、红绿灯和车道等基础能力验证。目标是跑通系统，而不是达到最终精度。

## 10.2 第二阶段以后

系统实际运行后，重点保存以下失败样本：

- 手机误报、服务器拒绝的事件；
- 服务器与手机违法类型不一致的事件；
- 夜间、雨天、逆光、隧道等环境中的漏检；
- 红绿灯、停止线和车道线识别错误；
- 车牌 OCR 多帧不一致；
- 人工确认模型判断错误的任务。

这些样本形成 Hard Example Dataset，用于后续微调基础模型。真正有价值的数据不是大量普通道路画面，而是模型最容易犯错的真实场景。

## 10.3 适合专项训练的行为

闯红灯、明显跨实线和逆行等行为优先使用基础模型 + 跟踪 + 几何/时序规则判断。

“变道未打转向灯”“危险变道”“加塞”等需要连续时间信息和车辆灯光状态的行为，后期可单独训练专项时序模型。模型必须允许 UNKNOWN 或无法确认状态，不能在证据不足时强制输出有违法或无违法。

---

# 11. 手机与服务器一致性门控

| 手机结果 | 服务器结果 | 系统动作 |
|---|---|---|
| A 类候选 | A 类通过 | 进入提交流程 |
| A 类候选 | B 类通过 | 不一致，拒绝提交 |
| A 类候选 | REJECTED | 拒绝提交 |
| 未达到上传阈值 | 不调用服务器 | 本地按缓存策略淘汰 |
| 人工强制送审 | 服务器通过且证据合格 | 进入提交流程 |
| 人工强制送审 | 服务器拒绝 | 不提交 |

服务器应真正执行独立判断，而不是简单复述手机结果。为了降低同源错误，服务器可以采用更高分辨率、更高采样密度、更完整模型或额外规则。

---

# 12. 服务器 Docker 化设计

服务器端从第一版开始采用 Docker Compose 管理。个人使用场景不需要 Kubernetes。

建议逻辑上拆分为以下服务：

| 服务 | 职责 |
|---|---|
| traffic-api | 接收上传、任务查询、记录查询、删除、健康检查 |
| traffic-worker | 视频解码、模型推理、OCR、规则判断 |
| database | 保存任务状态、结果、记录和配置 |
| optional-proxy | 如需要，可负责 HTTPS 和外部入口 |

第一阶段可以只构建 traffic-api 和 traffic-worker 两个自有镜像，数据库使用成熟官方镜像或 SQLite 文件。

## 12.1 数据与镜像分离

以下数据必须位于容器外部挂载目录或 Docker Volume 中：

- 任务视频和原始证据；
- 模型文件；
- 数据库；
- 举报结果记录；
- 日志；
- 配置文件。

模型不建议直接永久固化在业务镜像中。模型升级后应能够只替换模型文件和配置并重启 Worker，而不重新构建全部服务。

## 12.2 Docker 验收要求

服务器应能够完成以下迁移测试：停止原服务器容器、复制部署配置和数据目录到另一台 Linux 主机、重新启动服务，并继续查询既有任务记录和处理新任务。

容器被删除和重新创建时，任务、模型、数据库和历史记录不得丢失。

---

# 13. 微信侧控制与提交流程

微信侧控制独立封装为 SubmissionController，识别系统不得直接依赖具体微信页面结构。

正式方案采用三级控制思路：

1. **Android AccessibilityService 作为微信侧核心控制方案**，负责打开微信后的页面识别、导航、点击、输入、视频选择、提交和结果确认；
2. 当小程序自定义组件、Canvas 或 WebView 无法提供可靠 Accessibility 节点时，使用屏幕 OCR 或图像匹配作为定位兜底；
3. 微信 OpenSDK 或其他官方公开跳转能力仅作为**可选入口优化**。只有在实际验证目标入口具备合法、稳定的跳转权限时才使用，系统不得依赖 OpenSDK 访问或控制“蓉e行”。

OpenSDK 不承担小程序内部自动化控制职责。即使能够拉起目标小程序，后续页面导航、表单填写、视频选择和提交仍由 AccessibilityService 等手机侧 UI 自动化机制完成。

UiAutomator 或 Appium 可用于开发、调试和页面探索，但不作为正式运行时依赖。正式 App 不依赖 ADB、电脑、Root、Xposed 或 Frida。

## 13.1 微信入口策略

第一阶段不把 OpenSDK 作为必要依赖。Android 可通过系统方式启动微信，再由 AccessibilityService 根据页面状态逐步进入“成都交警”及“蓉e行”举报入口。

建议维护明确的页面状态机，例如：WECHAT_HOME → SEARCH_PAGE → CHENGDU_POLICE_ACCOUNT → RONG_E_XING_HOME → REPORT_ENTRY → REPORT_FORM → VIDEO_PICKER → FORM_READY → SUBMITTING → SUBMIT_SUCCESS。

每个状态必须通过多个页面特征共同确认，不允许仅凭固定坐标继续操作。若出现登录失效、版本更新、网络异常、验证码、风险确认或其他未知页面，应立即停止自动流程并进入 WAIT_USER 或 SUBMIT_FAILED 状态。

## 13.2 页面控制原则

AccessibilityService 优先根据文本、contentDescription、控件类型和父子关系定位按钮，而不是使用固定屏幕坐标。

每一步操作都必须执行“确认当前页面 → 查找目标控件 → 执行动作 → 验证目标页面或状态变化”的闭环。

对于 Accessibility 无法读取的页面，可截图后在手机本地执行 OCR，根据文字框位置执行点击。图像模板匹配仅作为进一步兜底，固定坐标属于最低优先级方案。

## 13.3 视频选择

最终证据视频可以注册到 Android MediaStore，并放入专用媒体目录。微信调用视频选择器时，SubmissionController 从该目录选择当前任务对应证据。

不建议通过修改微信内部路径、注入文件参数或模拟私有请求的方式上传视频。

## 13.4 验证码和风控

验证码、实名再次确认、微信风险验证或其他明确用于人工确认和安全控制的步骤不进行绕过。系统进入 WAIT_USER 状态，由用户完成必要确认后再继续。

## 13.5 提交成功判定

点击提交按钮不代表任务成功。SubmissionController 必须检测成功页、受理状态或举报记录，并将提交时间、可能存在的记录编号和最终状态回传服务器。

网络异常、入口变化、视频上传失败或微信登录失效时，任务进入失败或等待用户状态，不允许无条件连续重复提交。

---

# 14. 违法类型实施顺序

| 优先级 | 类型 | 原因 |
|---|---|---|
| P0 | 闯红灯 | 时序明确，红灯、停止线和车辆轨迹可形成稳定状态机 |
| P1 | 明显跨实线/压实线 | 几何关系相对直观 |
| P1 | 逆行、明显占用特定车道 | 道路结构清晰时规则较容易表达 |
| P2 | 变道未打转向灯 | 需要同时证明变道发生和转向灯未开启，受遮挡、曝光和 LED 频闪影响较大 |
| P3 | 危险变道、加塞等复杂行为 | 更依赖长时间序列和专项模型 |

---

# 15. 视频大小与证据画质

视频大小优先通过源录像码率和事件时长控制，而不是在提交前猛烈二次压缩。

以约 5 Mbps 为例，30 秒视频约 18.75 MB，60 秒约 37.5 MB，因此正常 20–35 秒事件通常可以在保持 1080p 画质的情况下满足 50 MB 级别限制。

处理优先级为：保证车牌可读和违法过程完整 → 缩短无关前后缓冲 → 调整未来录像源码率。除非平台明确允许，否则不把二次有损转码作为默认处理方式。

原始证据不绘制检测框、OCR 文本、水印或其他 AI 标记。AI 可视化结果属于调试数据，与证据文件分离。

历史公开规则曾出现视频大小和视频不得处理等要求，因此相关限制应设计成配置项，并在正式启用前以当前平台实际页面要求为准。

---

# 16. 服务器存储与举报记录

服务器只保存候选事件、举报任务和必要记录，不保存完整连续行车录像。

| 数据类型 | 建议默认保留 | 清理方式 |
|---|---|---|
| 手机上传但服务器拒绝 | 0–3 天 | 调试期短期保留，稳定后可立即删除视频 |
| 提交失败但可重试 | 3–7 天 | 成功或超过重试期限后清理 |
| 已成功提交事件 | 7–30 天，可配置 | 到期自动删除，可人工提前删除 |
| 举报回执和状态元数据 | 可比视频更长 | 保留必要文字信息 |
| 系统日志 | 7–14 天 | 滚动清理 |

后台需要支持按事件、日期和状态人工清理，同时支持只删除视频但保留举报状态元数据。

所有清理操作必须同时处理数据库索引、磁盘文件和缓存引用，避免留下失去索引的孤立证据文件。

---

# 17. 安全与隐私边界

- 手机与服务器之间使用加密传输；
- 服务端数据目录限制系统账户和容器访问权限；
- 不建立面向公众的车牌数据库；
- 不持续保存普通道路参与者的完整行车录像；
- 原始证据与 AI 分析副本分目录存储；
- 日志不得打印完整认证令牌、Cookie 或其他敏感登录信息；
- 微信侧自动化不得绕过验证码、实名确认和平台安全机制；
- 不把逆向私有接口、提取内部令牌、模拟签名算法或绕过风控作为正式架构的一部分。

---

# 18. 系统模块划分

| 模块 | 部署位置 | 职责 |
|---|---|---|
| RecorderService | Android | Camera2/CameraX、硬件编码、连续录像 |
| RingBuffer | Android | 管理滚动视频分片和时间轴 |
| MobileDetector | Android | 精简模型粗筛 |
| Tracker | Android | 轻量目标跟踪 |
| EventBuilder | Android | 冻结事件时间窗并生成任务包 |
| UploadManager | Android | 任务上传、断点和重试 |
| SubmissionQueue | Android | 等待服务器批准的提交任务队列 |
| SubmissionController | Android | 控制微信提交流程 |
| TaskAPI | 服务器 | 接收、查询、更新任务 |
| QueueManager | 服务器 | 管理待处理任务和恢复 |
| AnalysisWorker | 服务器 | 视频解码、模型和规则执行 |
| OCRService | 服务器 | 多帧车牌检测与识别 |
| DecisionEngine | 服务器 | 形成最终通过/拒绝结果 |
| RecordStore | 服务器 | 历史记录、证据、保留期和清理 |
| ModelStore | 服务器 | 管理可替换模型及版本 |
| Config | 两端 | 阈值、事件窗口、大小限制、保留期、功能开关 |

---

# 19. 4C12G 服务器资源策略

4 核 12 GB 服务器适合“手机先筛选、服务器按事件处理”的模式，不适合长期对 1080p30 全帧同时运行多个深度模型。

| 组件 | 资源策略 |
|---|---|
| API/任务管理 | 常驻低占用，优先保证响应性 |
| 视频解码 | 任务处理期间按需使用 |
| 车辆检测/跟踪 | 主要 CPU 消耗，单事件串行或低并发 |
| OCR | 只对目标车辆关键帧执行 |
| 复杂模型 | 按需调用，不持续运行全视频 |
| 内存 | 重点限制解码帧缓存和模型进程峰值 |
| 磁盘 | 至少长期保持 20–30% 可用空间 |

模型训练不在该 4C12G 服务器上执行。后续模型微调可使用临时 GPU 云实例或本地 GPU，训练结束后导出适合 CPU/移动端推理的模型重新部署。

---

# 20. 异常与降级策略

| 异常 | 系统行为 |
|---|---|
| 手机离线 | 本地继续录像和事件缓存，网络恢复后上传未过期任务 |
| 服务器不可用 | 任务进入待上传队列，不影响持续录像 |
| 上传中断 | 根据 event_id/task_id 安全重试，避免重复任务 |
| 服务器重启 | 从持久化数据库恢复 QUEUED/PROCESSING 任务 |
| 模型执行失败 | 标记 ERROR，根据错误类型决定重试或拒绝 |
| 手机与服务器结果不一致 | 不提交 |
| 车牌不可确认 | 不提交 |
| 视频文件过大 | 缩短非关键事件窗口，必要时转人工处理 |
| 微信入口变化 | 仅修改 SubmissionController 适配，不影响录像和模型 |
| 微信登录失效 | 进入 WAIT_USER，不自动尝试绕过 |
| 提交结果无法确认 | 标记状态未知，不自动视为成功 |
| 存储不足 | 优先删除过期/拒绝任务，再缩短普通环形缓存 |

---

# 21. 第一阶段验收标准

第一阶段完成时，不要求复杂违法识别已经达到实用精度，但以下链路必须完整跑通：

**手机或网页运行精简模型 → 产生候选事件 → Android 生成任务包 → 上传服务器 → 服务器持久化 → 加入任务队列 → Worker 完成模型验证 → 返回标准结果 → Android 获得结果 → 进入微信提交流程 → 获取提交反馈 → 将最终状态回传服务器 → 服务器可查询记录并按策略清理。**

另外必须满足以下工程要求：

- 服务器全部核心服务能够通过 Docker Compose 部署；
- 删除并重新创建容器不会丢失任务和历史记录；
- 服务能够迁移到另一台 Linux 主机；
- 模型文件能够独立升级；
- 手机、服务器和微信提交三个部分可以分别调试；
- 微信失败不会影响录像；
- 服务器失败不会导致事件录像立即丢失；
- 每个任务从创建到删除都有明确状态。

第一阶段完成后，再进入模型精度、道路环境适配和更多违法类型开发。这样后续无论换检测模型、OCR、视觉大模型还是服务器硬件，都只是替换系统的一部分，而不是重做整个项目。

---

# 22. 最终推荐方案

最终落地形态采用：

**Android 本地连续取证 + 手机轻量模型粗筛 + 事件任务包上传 + Docker 化服务器异步终判 + 手机双判定一致性门控 + 微信 Accessibility 自动化提交 + 服务器举报记录管理。**

第一阶段优先证明链路，而不是追求模型数量。端侧精简模型负责减少无效任务，服务器负责更严格验证，手机与服务器结果一致后才允许进入微信提交。

循环录像采用统一时间轴和短分片环形缓存，违法行为跨越多个视频分片时按事件时间范围组合，不把物理文件边界当作业务边界。

服务器从第一天采用 Docker 化、异步任务和持久化队列设计。模型属于可替换组件，不与 API 和任务系统强绑定。

公共模型先用于建立基础能力，实际运行中持续收集失败样本，再针对真实道路环境做微调。只有对未打转向灯变道、危险变道等复杂时序行为，后期再投入专项训练。

微信侧以 AccessibilityService 作为核心控制方案，OCR/图像识别作为节点不可访问时的兜底。OpenSDK 或其他官方跳转能力仅在实际验证目标入口具备合法、稳定跳转权限时作为可选优化，系统不得把 OpenSDK 视为控制任意第三方小程序的通用自动化接口，也不得依赖它完成“蓉e行”访问。验证码、实名和风险验证保留人工处理，不通过私有协议或安全绕过获得所谓“全自动”。

整个项目的实施核心不是一开始拥有最复杂的模型，而是先建立一个每个环节都可观察、可替换、可恢复、可迁移的完整系统。等这套骨架稳定以后，再让模型逐渐变聪明。否则很容易得到五个模型、三个推理框架和一个永远提交不出去的视频，这种收藏价值大于使用价值的工程作品。

---

# 23. 首版实施记录与调整（2026-09-17 起）

当前已完成端侧 EfficientDet-Lite0 车辆识别、手动/语音片段上传、独立 Docker 后端、SQLite 持久化队列、YOLOX-S 车辆分析、HyperLPR3 车牌 OCR、安卓结果回传，以及手机画面按车辆框叠加车型与车牌。安卓工程位于 `IllegalCapture/`，后端为同级独立目录 `backend/`；运行方式与接口见两者 README。进度总表见文首 2026-09-18 节。

实施中采用以下调整：

1. 手机候选类型允许 `UNKNOWN`，也可在稳定红灯 + 前车继续接近 + 车牌多帧一致时自动截取（`automatic`）。车辆存在不能单独作为违法触发条件。
2. 增加 `ANALYZED` 状态表示车辆分析完成，与“举报审核通过”区分。闯红灯三条件只产生 `RED_LIGHT` 候选，`submission_allowed=false`，不得进入提交。
3. 本机先使用 SQLite WAL 持久化任务兼队列，API 和 CPU worker 分开运行，数据和模型挂载到宿主机。多机扩展时再引入独立队列与对象存储。
4. 端侧和服务器采用固定、带哈希的官方模型导出版本，替换时必须同时验证预处理、输出解码和标签契约，不能只覆盖权重文件。
5. 1080p 车牌必须在原始分辨率上检测：整帧 640 会漏掉远处小牌；分块检出后对小牌邻域放大重读，多读数按合法字符数优先合并。手机实时路径另做近 4 次位置投票，纠正省份汉字单帧误读。

已验证真机八项测试（含环形缓存预热后片段 ≥14 秒）、真实视频上传与人工复核回传、夜间行车记录仪 00:00 段的稳定红灯观察、Docker 上传/UNCERTAIN 复核且 `submission_allowed=false`。00:00 段前车底边未下移（Δy2=−0.07）且机位判定为移动，因此不输出 `RED_LIGHT` 候选。尚未覆盖：停止线越线几何、微信提交反馈、远程主机迁移及长期道路测试。内置 `traffic.mp4` 来自静态交通照片，只用于链路验证；路况以 `test.mp4` 的 00:00 与 1:07 两段为准。
